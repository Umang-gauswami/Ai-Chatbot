# Rename this file to .env and put it in the project root.
# If you don't have an API key, the app will still run in "KB-only" mode.
OPENAI_API_KEY=sk-...
# Optional: choose the chat model (defaults to gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini